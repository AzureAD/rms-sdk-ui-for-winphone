<Page
    x:Class="RMSSampleApp.DefaultPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RMSSampleApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock  Text="RMS sample app" Margin="10,0,0,0"
                       VerticalAlignment="Bottom" HorizontalAlignment="Left"                       
                       FontFamily="Segoe UI" FontWeight="Light" FontSize="45">
            </TextBlock>
        </StackPanel>
        <StackPanel Orientation="Vertical" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Stretch">
            <Button Content="Protect Text Demo" Name="ProtectDemoButton"  Margin="10,0" Click="ProtectDemoButton_Click"></Button>
        </StackPanel>
    </Grid>
</Page>
