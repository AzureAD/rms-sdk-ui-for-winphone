<Page
    x:Class="RMSSampleApp.TextProtectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RMSSampleApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Vertical">
            <TextBox x:Name="TextContent" Height="400" Width="380" AcceptsReturn="True" FontSize="15" HorizontalAlignment="Left" Margin="10,0"  FontFamily="Segoe UI"></TextBox>
            <Button Content="Protect" Name="ProtectButton" Click="ProtectButton_Click" Margin="10,0"></Button>
        </StackPanel>

    </Grid>
</Page>
